<tr>
  <td><%= answer.body %></td>
  <td><%= t(".correct.#{answer.correct}") %></td>
  <td><%= link_to t('shared.show'), admin_answer_path(answer) %></td>
  <td><%= link_to t('shared.edit'), edit_admin_answer_path(answer) %></td>
  <td><%= link_to t('shared.delete'), admin_answer_path(answer), method: :delete, data: { confirm: t('shared.are_you_sure') } %></td>
</tr>
