<p>
  <strong>Test:</strong>
  <%= @test.title %>
</p>

<p>
  <strong>Category:</strong>
  <%= @test.category.title %>
</p>

<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p>

<table>
  <thead>
  <tr>
    <th>Question</th>
    <th>Answers count</th>
    <th colspan="3">Actions</th>
  </tr>
  </thead>

  <tbody>
  <%= render @test.questions %>
  </tbody>
</table>
<br>

<%= link_to 'New question', new_admin_test_question_path(@test) %> |
<%= link_to 'Edit test', edit_admin_test_path(@test) %> |
<%= link_to 'Delete test', admin_test_path(@test), method: :delete, data: { confirm: 'Really?' } %> |
<%= link_to 'Back', admin_tests_path %>
