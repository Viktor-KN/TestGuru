<%= render 'form' %>

<div class="row justify-content-center">
  <div class="col col-md-8 nav">
    <%= link_to t('shared.show'), admin_question_path(@question), class: 'nav-link' %>
    <%= link_to t('shared.back'), admin_test_path(@question.test), class: 'nav-link' %>
  </div>
</div>
