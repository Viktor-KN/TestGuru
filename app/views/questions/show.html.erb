<h2><%= @question.body %></h2>
<ul>
  <% @question.answers.each do |answer| %>
    <li>
      <%= answer.body %>
    </li>
  <% end %>
</ul>
<%= link_to 'Delete', question_path(@question), method: :delete %>
